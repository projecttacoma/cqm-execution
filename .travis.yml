language: node_js
install:
  - npm install -g codecov
node_js:
  - "node"
  - "lts/*"
  - "9"
cache:
  directories:
    - "node_modules"
script:
  - yarn install
  - yarn run lint
  - yarn test
  - codecov
